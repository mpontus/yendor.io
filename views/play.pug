extends layout

block content
  h1= title
  .game-container
  script.
    Yendor.play(document.querySelector('.game-container'), {
      game: "#{game}",
      cols: #{term.cols},
      rows: #{term.rows},
    });
  .row
    .col-md-8
      h2 Controls
      pre #{readme}